#nested function calls = function calls  inside other functionn calls.
#  The inner function is executed first,
#  and its result is used as an argument for the outer function.

#num = input("Enter a whole positive number: ")
#num = float(num)
#num = abs(num)
#num = round(num)
#print(num)

print((round(abs(float(input("Enter a whole positive number:"))))))

            